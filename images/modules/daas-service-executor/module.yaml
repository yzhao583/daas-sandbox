schema_version: 1

name: "org.kie.daas.service.executor"
version: "0.1"
description: "KIE DaaS service executor module"

labels:
  - name: "io.openshift.expose-services"
    value: "8080:http"

ports:
    - value: 8080

execute:
  - script: "module.sh"
