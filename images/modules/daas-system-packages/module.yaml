schema_version: 1

name: "org.kie.daas.system.packages"
version: "0.1"
description: "KIE DaaS system packages module"

# TODO: remove unnecessary packages, depending on which image
#       (findutils, git, procps-ng, vim-minimal, which, ...)
packages:
  install:
    - file
    - findutils
    - git
    - gzip
    - procps-ng
    - tar
    - vim-minimal
    - which
