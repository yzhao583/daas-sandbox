schema_version: 1

name: "org.kie.daas.www.nginx"
version: "0.1"
description: "KIE DaaS www nginx module"

labels:
  - name: "io.openshift.expose-services"
    value: "8080:http"

packages:
  install:
    - nginx

ports:
    - value: 8080

execute:
  - script: "module.sh"
